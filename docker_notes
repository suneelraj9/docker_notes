Build the docker image
**********************
 1. mkdir /home/suneel/mywebsite
 2. vi index.html
    <html>
    <body>
    This is my test page
    </body>
    </html>
 3. Vi Dockerfile
    FROM httpd
    RUN rm /usr/www/html/index.html
    ADD ./index.html /usr/www/html/
 4. docker build . -t mytestimage
 5. docker run -it -p 8080:80 -d <Image ID>
 6. docker exec -it <image id> bash
 
